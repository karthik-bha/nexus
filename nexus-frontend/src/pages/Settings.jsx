import { Edit, Plus } from "lucide-react";
import Navbar from "../components/Navbar"

/// What to display
// Settings
// pfp in center with a edit option
// username with edit icon
// email if available else just a gentle nudge to input if needed (provide add and edit option for non-existent ones)
// reset password
// bio with add/edit/delete option
// a small message thanking them for using the platform
const Settings = () => {
    
    return (
        <div>
    
            <h1>Settings</h1>
        </div>
    )
}

export default Settings;